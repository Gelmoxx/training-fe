export class Brand {
  id?: number;
  brandName: string;


  constructor(brand: Brand) {
    this.id = brand.id;
    this.brandName = brand.brandName;
  }
}

export class BrandList{
  id?: number;
  brandName: string;


  constructor(brand: Brand) {
    this.id = brand.id;
    this.brandName = brand.brandName;
  }
}

